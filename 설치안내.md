# 리듬맵 — 오프라인 설치 안내

이 폴더 전체가 하나의 앱입니다. 파일을 따로 떼지 말고 폴더째로 다루세요.

```
rhythmmap/
├─ index.html              ← 앱 본체
├─ manifest.webmanifest    ← 앱 정보(이름·아이콘)
├─ sw.js                   ← 오프라인 캐싱 담당
├─ icon-192.png            ← 앱 아이콘
└─ icon-512.png            ← 앱 아이콘(고해상도)
```

---

## 바탕화면에 앱으로 설치하기 (권장)

서비스워커(오프라인 기능)는 보안상 **웹주소(https)** 에서만 작동합니다.
파일을 그냥 더블클릭하면 지도·입력은 되지만 오프라인 캐싱과 "설치"가 안 됩니다.
그래서 한 번 웹에 올린 뒤 설치하는 방식을 권합니다. (수업관리 앱과 같은 방식)

### 1) GitHub Pages에 올리기
1. GitHub에 새 저장소를 만들고 이 폴더의 5개 파일을 모두 올립니다.
2. Settings → Pages → Branch를 `main` / `/ (root)` 로 지정하고 저장.
3. 잠시 뒤 `https://(아이디).github.io/(저장소이름)/` 주소가 생깁니다.

### 2) 노트북 바탕화면에 추가 (Chrome / Edge)
1. 위 주소를 크롬이나 엣지로 엽니다. (한 번은 인터넷 연결 상태로)
2. 주소창 오른쪽 끝에 뜨는 **설치 아이콘(⊕ 모양)** 을 누릅니다.
   - 안 보이면: 우측 상단 ⋮ 메뉴 → "앱 설치" 또는 "리듬맵 설치".
3. "설치"를 누르면 바탕화면(과 시작 메뉴)에 **리듬맵** 아이콘이 생깁니다.
4. 이제 그 아이콘으로 열면 브라우저 주소창 없이 독립 창으로 뜹니다.

설치 후 **온라인 상태로 한 번** 지도를 이리저리 움직여 두면, 그 지역 지도가
캐시되어 다음부터 오프라인에서도 보입니다.

---

## 오프라인에서 되는 것 / 안 되는 것

**오프라인에서도 됩니다**
- 앱 열기, 기관 추가·일괄입력(좌표 입력분), 유형 수정·추가·삭제
- 영업 단계 변경, 강사 배분, 메모, 침투율·목표 계산
- 저장하기(JSON 내려받기) / 불러오기
- 한 번이라도 봤던 지역의 지도

**온라인이어야 됩니다**
- 주소 → 좌표 자동 변환(주소 검색): 인터넷 연결 필요
- 아직 한 번도 안 본 지역의 지도 타일: 오프라인이면 회색으로 표시
  → 오프라인 작업 예정이면, 미리 온라인일 때 해당 지역을 둘러봐 두세요.

헤더 오른쪽의 **온라인/오프라인 표시등**으로 현재 상태를 확인할 수 있습니다.

---

## 데이터는 어디에 있나요?
입력한 기관·유형·목표는 앱이 켜져 있는 동안 유지됩니다.
작업이 끝나면 꼭 **"저장하기"** 로 JSON 파일을 내려받아 보관하세요.
다음에 **"불러오기"** 로 그 파일을 올리면 그대로 복구됩니다.
(여러 기기·여러 강사가 공유하려면, 나중에 이 JSON을 클라우드에 두거나
 Firebase 연동 버전으로 확장하면 됩니다.)
